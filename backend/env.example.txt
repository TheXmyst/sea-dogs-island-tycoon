# Database Configuration
# Pour Railway, utilisez DATABASE_URL ou les variables PG_* automatiques
DB_USER=postgres
DB_HOST=localhost
DB_NAME=seadogs
DB_PASSWORD=your_password_here
DB_PORT=5432

# Server Configuration
PORT=5000
NODE_ENV=development

# Security Configuration
# IMPORTANT: Générer une clé secrète forte (minimum 32 caractères aléatoires)
# Commande pour générer: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_secret_key_here_minimum_32_characters_long_and_random

# Frontend URL (pour CORS en production)
# En développement, laisser vide pour permettre toutes les origines
# En production (Railway), spécifier l'URL du frontend Vercel
# Exemples:
#   - URL unique: https://votre-app.vercel.app
#   - Plusieurs URLs: https://app.vercel.app,https://app-git-main.vercel.app
#   - Avec wildcard pour previews Vercel: https://*.vercel.app,https://votre-domaine.com
# IMPORTANT: Cette URL doit correspondre exactement à l'URL de votre frontend Vercel
FRONTEND_URL=

